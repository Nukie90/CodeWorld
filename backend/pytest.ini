[pytest]
addopts = -ra --strict-markers --cov=app --cov-report=term-missing --cov-report=xml
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    unit: isolated low-level tests
    integration: FastAPI route and service integration tests
filterwarnings =
    ignore::DeprecationWarning
